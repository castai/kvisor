# AWS Configuration for Integration Tests
# Copy this file to .env and fill in your values

# Required: AWS Profile from ~/.aws/config or ~/.aws/credentials
# The profile contains region and credentials configuration
AWS_PROFILE=default

# Required: EC2 Instance ID to test volume listing
# Example: i-1234567890abcdef0
AWS_INSTANCE_ID=

# Optional: AWS Account ID
AWS_ACCOUNT_ID=

# Optional: Path to AWS credentials file
# If not set, will use default credential chain (IAM roles, env vars, ~/.aws/credentials)
# AWS_CREDENTIALS_FILE=/path/to/credentials
# AWS_SHARED_CREDENTIALS_FILE=~/.aws/credentials

# Optional: AWS credentials (alternative to profile)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=
# AWS_REGION=us-east-1
